3. Generate a random permutation of a given array and its size. 

class RandomPermutation{
public:
	vector<int> ComputeRandomPermutation(int n){
		vector<int> perm(n,0);
		iota(permutation.begin(),permutation.end(),0)
		RandomSampling s;
		Return s.Solution(perm,n);
	}
}; 
